class TchatcheController < ApplicationController
  def profile
    @user=User.find params[:id]
    render layout: "sometemplate"
  end

  def chat
    @user=User.find params[:id]
    render layout: "sometemplate"
  end
end
